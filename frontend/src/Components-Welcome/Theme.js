
import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
    palette: {
      primary: {
        mode: "dark",
        main: '#BE2424'
      },
      secondary: {
        main:'#FFFFFF'
      },
    },
  });

  export default Theme;